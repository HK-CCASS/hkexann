# 执行报告：GitHub 上传准备

**时间**：2026-03-04_22-24-47
**状态**：✅ 全部完成

---

## 目标

将 hkexann 项目准备好上传到 GitHub，使 Mac Mini 可通过 Claude Code 直接部署。

---

## 完成内容

### 1. requirements.txt 补全

| 新增包 | 原因 |
|--------|------|
| `python-dotenv>=0.19.0` | 加载 .env 环境变量 |
| `marshmallow<4.0` | pymilvus 兼容性约束 |
| `pytz>=2021.1` | 香港时区处理 |
| `pandas>=1.3.0` | 数据处理 |
| `opencc-python-reimplemented>=0.1.1` | 繁简关键字匹配 |
| `pydantic-settings>=2.0.0` | config/settings.py 依赖 |
| `clickhouse-driver>=0.2.0` | ClickHouse 连接（可选） |

### 2. README.md 完整重写

内容覆盖：
- 系统架构图与关键路径表
- 前置要求（macOS / Docker / SSD）
- 8 步部署流程（第一步：确认 SSD → 第八步：冒烟测试）
- 配置说明（config.yaml + .env 字段表）
- 日常使用：历史批量回填（参数表 + 断点续传说明）、实时监听
- Milvus 维护（compaction）
- launchd 自动化定时任务
- 数据迁移（SSD 随盘迁移方案）
- 项目结构树

### 3. scripts/com.hkexann.nightly.plist 创建

- README 引用的 plist 文件之前缺失，现已补充
- 路径使用 `/path/to/your/hkexann` 占位符
- UTC 时间 18:00（= HKT 02:00）触发
- README 中补充了"需编辑 plist 路径"的说明

### 4. .gitignore 审查

| 项目 | 状态 |
|------|------|
| `.env` | ✅ 已排除 |
| `hkexann/`（含 checkpoint） | ✅ 已排除 |
| `.claude/` | ✅ 已排除 |
| `*.log` / `logs/` | ✅ 已排除 |
| 硬编码密钥 | ✅ 无 |
| `nightly_download.sh` 个人路径 | ⚠️ 含 `/Users/jet/...`（非安全问题，README 已说明需修改） |

---

## 上传前操作步骤

在终端执行（在主仓库目录，非 worktree）：

```bash
cd /Users/jet/hkexann

# 1. 合并 worktree 分支到 master
git checkout master
git merge claude/loving-bardeen

# 2. 推送到 GitHub
git push origin master

# 3. 清理 worktree（可选）
git worktree remove .claude/worktrees/loving-bardeen
```

---

## Mac Mini 部署入口

克隆后直接交给 Claude Code：

```
请按照 README.md 的部署指南为我完整部署这个项目。
外接 SSD 已挂载在 /Volumes/SSD/，Milvus 数据已在 SSD 上。
```
